# Backend

Placeholder for the MoneyShot backend (Hermes agent + the auth/SSE bridge "bff"
that verifies the Google ID token and relays to Hermes over an OpenAI-compatible
API). Not published here yet. Secrets (`*.env`) must never be committed.
