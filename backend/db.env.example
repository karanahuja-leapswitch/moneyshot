# Postgres container env — copy to db.env and set a strong password. Server-only, gitignored.
# Must match the password in DATABASE_URL (hermes.env). Generate: openssl rand -hex 24
POSTGRES_PASSWORD=REPLACE_WITH_DB_PASSWORD
